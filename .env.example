# Copy this file to .env and fill in your API key
OPENAI_API_KEY=

# Optional: Set to "ollama" for local LLM, "openai" for GPT-4o (default)
# LLM_PROVIDER=openai

# Optional: Set to "ollama" for local embeddings via nomic-embed-text (no download)
# EMBEDDING_PROVIDER=ollama
